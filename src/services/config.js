// Configuration Firebase (réexportée sous le nom par défaut 'firebaseConfig' 
// à partir du fichier config-secret.js placé dans le même répertoire)
export { default } from './config-secret';

// Nom de la collection principale
export const collUtil = "memo-utilisateurs";
// Nom de la sous-collection
export const collTaches = "memo-taches";